<div class="navbar-top">
  <div class="nav-grid">
    <% @url = request.path_info %>
    <% if @url.include?("#{params[:controller]}/#{params[:id]}") %>
      <%= link_to "#{params[:controller]}", "/#{params[:controller]}", class: "first" %>
      <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete} %>
    <% else %>
      <%= link_to 'Hey Baby', root_path, class: "center logo"%>
      <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete} %>
    <% end %>
  </div>
</div>
